# Backend API base URL (must match Partylist-Backend PORT in .env, default 3002)
API_URL=http://localhost:3002

# Public URL of this admin app (used for redirects / future callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3001
